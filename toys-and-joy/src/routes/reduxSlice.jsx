 
 import { createSlice } from "@reduxjs/toolkit";


 const reduxSlice = createSlice({
    name: 'sendItem',
    initialState: [],
    reducers: {
        cartItem: (state , action) => {
            state
        }
    }
 })